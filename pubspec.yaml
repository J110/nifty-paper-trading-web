name: nifty_paper_trading
description: Paper trading app for Nifty options models

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  dio: ^5.4.0
  fl_chart: ^0.66.0
  intl: ^0.19.0
  google_fonts: ^6.1.0
  go_router: ^13.0.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
